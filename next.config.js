/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: Add a basePath if you're deploying to a subpath
  // basePath: '/my-next-app',
};

module.exports = nextConfig;
